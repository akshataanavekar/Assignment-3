public class ReplaceCharOccurence 
{
	public static replaceCharacter(char replacment,String[] arrayofstring)
	{
		for(int index=0;index < arrayofstring.length;index++)
		arrayofstring[i].replace('a',replacement);
	}
    
}